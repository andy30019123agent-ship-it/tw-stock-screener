# 候選情報榜 第一段「誠實化」實作計畫

> **For agentic workers:** 逐 Task 執行，每個 Task 結束都要跑 `python3 -m pytest pipeline/tests -q` 並看到全綠才 commit。
> 規格來源：`docs/需求規格_每日候選榜_2026-07-26.md`（Andy 2026-07-26 選路線 4️⃣）。

**Goal：** 把產品從「每日 Top 8 購買組合」改成「每日候選情報清單」，並把所有績效數字改成 Andy 真正買得到的口徑（隔日開盤進場、扣成本）。

**Architecture：** 後端先改回測進場口徑（影響所有下游數字）→ 產生「條件層級」統計 → `opportunities.py` 輸出全部候選與分類 → 前端拆掉組合語意、改讀層級統計。

**Tech Stack：** Python 3（無外部套件，pytest）、React + Vite。

## Global Constraints

- **成本**：來回 0.785%（手續費 0.1425%×2 + 證交稅 0.3% + 滑價 0.1%×2）。常數已存在 `backtest_signals.py:1025-1027`，不要重新定義。
- **持有窗**：`FORWARD = 20` 個交易日。
- **進場價**：`還原後 open[i+1]`（訊號日的隔天開盤）。**出場價**：`還原後 close[i+forward]`。這樣持有期正好是 forward 個交易日。
- **勝率**一律指「成本後絕對報酬 > 0 的比例」。「贏大盤機率」必須另外命名為 `beat_market_rate`，前端不得叫它勝率。
- **不得**顯示個股精確勝率，只顯示所屬層級的歷史分佈。
- **不得**新增任何「建議買幾檔／買多少錢／建議賣出日」的文案。
- 前端數字**不得寫死**，一律讀 JSON（現有 `OutcomeShape.jsx` / `HowManyToBuy.jsx` 的寫死常數是這條規則的反面教材）。
- 繁體中文文案，中英數之間加半形空格。

---

### Task 1: 回測進場價改成「隔日開盤」

**Files:**
- Modify: `pipeline/backtest_signals.py`（`collect_events` 約 369-455、`exit_analysis` 約 1036-1130）
- Test: `pipeline/tests/test_backtest_signals.py`

**Interfaces:**
- Produces: `collect_events()` 回傳的每個 event 的 `ret` 改為 `adj_close[i+forward] / adj_open[i+1] - 1`；`by_date[di]` 基準母體用同一口徑。

- [ ] **Step 1: 寫失敗測試**

在 `pipeline/tests/test_backtest_signals.py` 加：

```python
def test_進場價用隔日開盤而非當日收盤():
    """Andy 收到名單時已收盤，最快隔天開盤才買。回測必須用同一把尺。"""
    # 造一檔：訊號日收盤 100，隔日開盤 110（跳空 10%），第 20 日收盤 121
    rows = _make_rows(n=90, close=100.0)
    i_sig = 60
    rows[i_sig + 1]["open"] = 110.0
    rows[i_sig + 20]["close"] = 121.0
    price_hist = {"9999": _rows_to_hist(rows)}
    events, by_date = bt.collect_events(price_hist, {}, {}, [{"id": "9999", "name": "測試"}])
    ev = [e for e in events if e["i"] == i_sig]
    assert ev, "訊號日應有事件"
    # 用隔日開盤 110 進場 → 121/110-1 = 10%；用當日收盤 100 進場會是 21%
    assert abs(ev[0]["ret"] - 0.10) < 1e-6, f"實際 {ev[0]['ret']}"
```

（`_make_rows` / `_rows_to_hist` 若不存在就照該測試檔既有的造資料 helper 寫，不要另創風格。）

- [ ] **Step 2: 跑測試確認失敗**

Run: `python3 -m pytest pipeline/tests/test_backtest_signals.py -k 隔日開盤 -v`
Expected: FAIL（實際值會是 0.21）

- [ ] **Step 3: 實作**

在 `collect_events` 內，把
```python
adj_close = [r["close"] for r in bd.back_adjust_rows(pr, ev)]
```
改成
```python
_adj = bd.back_adjust_rows(pr, ev)
adj_close = [r["close"] for r in _adj]
adj_open = [r["open"] for r in _adj]
```
把迴圈內
```python
base = adj_close[i]
...
ret = adj_close[i + forward] / base - 1
```
改成
```python
# 進場價＝隔日開盤：管線 18:17（收盤後）才發榜，Andy 最快隔天開盤才買得到。
# 用訊號日收盤當進場價等於偷看——實測候選股隔夜跳空中位 +0.42%（全市場 +0.29%），
# 會讓每筆交易的績效虛高約 0.5pp。出場仍用 close[i+forward]，持有期正好 forward 天。
base = adj_open[i + 1]
...
ret = adj_close[i + forward] / base - 1
```
並在 `if not base: continue` 保留（開盤價可能是 0/None）。

`exit_analysis`（約 1036-1130）內同樣把持有報酬的起點從 `序列[i]` 改成 `隔日開盤`，MAE 的基準價一併改；註解說明理由。

- [ ] **Step 4: 跑全部測試**

Run: `python3 -m pytest pipeline/tests -q`
Expected: 全綠（既有測試若因口徑改變而失敗，**要逐個判斷是「測試凍結了舊口徑」還是「真的壞了」**，前者更新期望值並在測試裡註明為什麼改，後者修程式）。

- [ ] **Step 5: 產生改口徑前後對照數字**

Run: `python3 -c "..."` 或寫一次性腳本 `_reports/` 用，輸出「主要訊號的成本後超額：舊口徑 vs 新口徑」。存成 `_reports/口徑更正_隔日開盤_2026-07-26.md`。

- [ ] **Step 6: Commit**

```bash
git add pipeline/backtest_signals.py pipeline/tests/test_backtest_signals.py _reports/口徑更正_隔日開盤_2026-07-26.md
git commit -m "fix(backtest): 進場價改隔日開盤，修正全站績效虛高約 0.5pp"
```

---

### Task 2: 事件帶上特徵值（供層級統計）

**Files:**
- Modify: `pipeline/backtest_signals.py`（`collect_events`）
- Test: `pipeline/tests/test_backtest_signals.py`

**Interfaces:**
- Produces: 每個 event 多一個 `feat` dict：`{"turnover": float, "bias20_pct": float|None, "high20_gap": float|None, "rs_pct_60": float|None}`
  - `turnover` = `ind["avg_vol_lots"] * ind["close"]`（**不可用 `avg_money_e`**：`bt_to_price_hist` 把成交金額補 0，回測母體裡它恆為 0，見 `history_store.py:214`）
  - `high20_gap` = `(recent_high20 - close) / close * 100`（離前高多遠，正值＝還沒到前高）

- [ ] **Step 1: 寫失敗測試** — 斷言 event 有 `feat` 且 `turnover` 等於 `avg_vol_lots × close`。
- [ ] **Step 2: 跑測試確認失敗。**
- [ ] **Step 3: 實作** — 在 `events.append({...})` 內加 `"feat": {...}`，值全部取自當下的 `ind`（point-in-time，不可用最新值）。
- [ ] **Step 4: `python3 -m pytest pipeline/tests -q` 全綠。**
- [ ] **Step 5: Commit** — `feat(backtest): 事件帶 point-in-time 特徵值，供條件層級統計`

---

### Task 3: 新模組 `pipeline/tier_stats.py`（條件層級的歷史分佈）

**Files:**
- Create: `pipeline/tier_stats.py`
- Create: `pipeline/tests/test_tier_stats.py`
- Modify: `pipeline/opportunities.py`（`run()` 內呼叫並寫出 JSON）

**Interfaces:**
- Produces: `compute_tier_stats(events, cost=0.00785, block=20) -> dict`
  ```
  {
    "cost_pct": 0.785,
    "features": {
      "turnover":   {"high": {...}, "mid": {...}, "low": {...}},
      "bias20_pct": {...}, "high20_gap": {...}, "rs_pct_60": {...}
    }
  }
  ```
  每個層級 `{...}` 內含：`samples`(事件數)、`days`(不同進場日數)、`blocks`(days/20 取整＝約當獨立批次)、`win_rate`(成本後 >0 比例)、`median_pct`、`p20_pct`、`p80_pct`、`avg_win_pct`、`avg_loss_pct`、`ci90`([下界, 上界]，用長度 20 的 moving-block bootstrap，400 次)。

**設計要點（寫進 docstring）：**
- 分層是**同日**切三分位（同日比較才公平，跨日比會混進大盤漲跌）。
- 成本後淨報酬 = `ret - cost`。
- `blocks` 必須輸出：20 日持有窗高度重疊，`samples` 會嚴重高估獨立性。前端要顯示它。
- bootstrap 以「日」為單位重抽（moving block，長度 20），不是以事件為單位——同一天的事件不獨立。

- [ ] **Step 1: 寫失敗測試**（`test_tier_stats.py`）
  - `test_同日切三分位不跨日比較`：造兩天資料，第 1 天全體 turnover 都低但報酬高、第 2 天相反，斷言 high/low 分層是各日各切。
  - `test_成本已扣`：造 ret 全部 = 0.005（0.5%）的事件，`cost=0.00785` → `win_rate` 應為 0。
  - `test_blocks_等於不同日數除以20`。
- [ ] **Step 2: 跑測試確認失敗。**
- [ ] **Step 3: 實作 `pipeline/tier_stats.py`。**
- [ ] **Step 4: 測試全綠。**
- [ ] **Step 5: 接進 `opportunities.run()`**，寫出 `public/data/tier_stats.json` 與 `pipeline/tier_stats.json`（沿用 `_write_json`）。
- [ ] **Step 6: 實跑一次真資料**：`cd pipeline && python3 -c "import opportunities"...` 或直接跑 `build_data.py --skip-fetch`（若有此旗標；沒有就寫最小驅動腳本），確認 JSON 產出且 `blocks` 是合理數字（總天數約 400 → blocks 約 20）。
- [ ] **Step 7: Commit** — `feat(pipeline): 新增條件層級歷史分佈 tier_stats`

---

### Task 4: 成績單改成本後口徑

**Files:**
- Modify: `pipeline/opportunities.py:231-258`（`compute_scoreboard`）
- Test: `pipeline/tests/test_opportunities.py`

- [ ] **Step 1: 寫失敗測試** — 造一筆報酬 +0.5% 的舊 pick，斷言 `win_rate == 0`（扣 0.785% 後是負的），且回傳含 `cost_pct` 欄位。
- [ ] **Step 2: 確認失敗。**
- [ ] **Step 3: 實作** — `ret` 改 `ret - COST`；進場價改用隔日開盤（`adjusted_close_map` 之外另建 `adjusted_open_map`）；回傳多 `cost_pct` 與 `beat_market_rate`（若算得出來，算不出來就不要放假欄位）。
- [ ] **Step 4: 全測試綠。**
- [ ] **Step 5: Commit** — `fix(scoreboard): 勝率改成本後口徑，進場價改隔日開盤`

---

### Task 5: `opportunities.py` 輸出全部候選 + 特徵百分位 + 三分類

**Files:**
- Modify: `pipeline/opportunities.py`（`build_opportunities`、`TOP_N`）
- Test: `pipeline/tests/test_opportunities.py`

**Interfaces:**
- Produces: `opportunities.json` 的 `picks` 改為**全部合格候選**（不截斷），每檔多這些欄位：
  - `feat_pct`: `{"turnover": 0~1, "bias20_pct": 0~1, "high20_gap": 0~1, "rs_pct_60": 0~1}`（**當日候選池內**的百分位）
  - `tier`: `"win"`（勝率偏優）/ `"both"`（雙優）/ `"return"`（報酬偏優）/ `null`
  - 頂層多 `preview_n: 10`（前端預設顯示幾筆，**不是建議買幾檔**）與 `pool_n`
- 分類規則（Phase 1 用單一已驗證特徵，不做合成分數）：
  - `win_flag` = `turnover` 百分位 ≥ 0.67
  - `ret_flag` = `bias20_pct` 百分位 ≥ 0.67
  - 兩個都真 → `both`；只有 win → `win`；只有 ret 且 `turnover` 百分位 ≥ 0.33（勝率不在後段）→ `return`；其餘 `null`

- [ ] **Step 1: 寫失敗測試** — 造 10 檔候選，斷言 (a) 輸出不被截成 8 檔 (b) 分類符合上述規則 (c) `preview_n` 存在。
- [ ] **Step 2: 確認失敗。**
- [ ] **Step 3: 實作**：刪 `TOP_N` 截斷（`picks[:TOP_N]` → `picks`），把 `TOP_N` 常數與那段 40 行「買幾檔」註解一併移除，改成 `PREVIEW_N = 10` 並註明「這是畫面預設顯示筆數，不是建議買幾檔」。
- [ ] **Step 4: 全測試綠。**
- [ ] **Step 5: Commit** — `feat(opportunities): 輸出全部候選與三分類，移除 Top N 組合語意`

---

### Task 6: TG 快報改三分類摘要

**Files:**
- Modify: `pipeline/notify_tg.py`
- Test: `pipeline/tests/test_notify_tg.py`

- [ ] **Step 1: 寫失敗測試** — 斷言訊息 (a) 不含「精選」「建議買」「Top」等字 (b) 含三個分類標題 (c) 含固定提醒「這不是購買組合；單檔最好層級的歷史賺錢機率也只有約 52%」 (d) 零候選時仍能分辨「真的零檔」與「管線故障」（既有行為，不可回歸）。
- [ ] **Step 2: 確認失敗。**
- [ ] **Step 3: 實作。**
- [ ] **Step 4: 全測試綠。**
- [ ] **Step 5: Commit** — `feat(notify): 快報改候選情報三分類`

---

### Task 7: 前端拆掉組合語意

**Files:**
- Delete: `src/components/HowManyToBuy.jsx`
- Modify: `src/components/PositionPlan.jsx`（移除資金配置、買幾張、持有到期日；保留並改名觀察位）
- Modify: `src/lib/position.js`（移除 `targetPrice` 的 `TARGET_PCT` 統計目標、移除 `volatilityParity`、`defendPrice` 結論性文案）
- Modify: `src/components/Opportunities.jsx`（移除引用）

**改名規則（逐字）：**
- 「目標價」→「近 20 日前高（觀察位）」
- 「出場價」→ 移除（Phase 3 出場研究做完才會有正確版本）
- 「持有到期日」→「20 日績效檢視日」
- 「賺賠空間」→ 移除（兩把不同的尺算出來的比值，Andy 昨天覺得怪的就是這個）

- [ ] **Step 1: 先 grep 全部引用**：`grep -rn "HowManyToBuy\|volatilityParity\|targetPrice\|defendPrice\|目標價\|出場價\|賺賠空間\|持有到期" src/`，把清單寫進本 Task 的筆記。
- [ ] **Step 2: 逐個移除／改名。**
- [ ] **Step 3: `npm run build`** — 必須 0 error（有 `src/lib/__tests__/` 但沒有 test runner，build 通過＋下一步實跑就是驗收）。
- [ ] **Step 4: 實跑驗證** — `npm run build && python3 -m http.server -d dist 4173`，用 CDP 開 `http://localhost:4173/tw-stock-screener/?nocache=1`（390 寬），截圖確認：沒有「買幾檔」「配置金額」「目標價」「出場價」字樣。⚠️ 不可用 `vite preview`（headless 會整頁空白）。
- [ ] **Step 5: Commit** — `refactor(ui): 移除購買組合語意（買幾檔/資金配置/目標價/出場價）`

---

### Task 8: 前端候選清單 + 層級分佈卡

**Files:**
- Modify: `src/components/Opportunities.jsx`（卡片改成候選清單、加分類標籤與展開全部）
- Rewrite: `src/components/OutcomeShape.jsx` → 改讀 `tier_stats.json`，顯示「這個層級的歷史分佈」
- Modify: `src/App.jsx`（fetch `tier_stats.json`）

**必須出現在畫面上的三句話（逐字，不得省略）：**
1. 「這不是購買組合，是候選情報。買哪幾檔、買不買，由你決定。」
2. 「成交金額最高的三分之一，單檔歷史賺錢機率 48.6%；最低的三分之一 41.4%——**三層都不到一半**。這套的優勢在賺賠幅度（賺時 +16.3%、賠時 −9.9%，期望值 +2.85%），不是在勝率。」
3. 每個統計數字旁必須同時顯示 `blocks`（約當獨立批次數，目前是 19）與 90% 信賴區間。

**🔴 2026-07-26 重算後的硬性限制（違反就是用介面說謊）：**
- 數字**一律讀 `tier_stats.json`**，不得寫死。舊的 52%／44%／66% 是「當天收盤進場」時代的數字，全部作廢。
- **不得顯示 `high20_gap`（離前高多遠）為正面因子**：重算後它的方向與舊研究相反（high 42.5% vs low 45.8%），是不穩定特徵。可以顯示數值當事實，但不可標成「偏優」或加分。
- **不得用兩個層級的信賴區間是否重疊來宣稱差異顯著**。CI 只說明「這一層自己有多不確定」；要證明層級之間有差，需要的是同日配對比較，那是第二段 walk-forward 的工作。目前 turnover 高層 CI90 是 [44.3, 54.6]、低層 [36.0, 47.2]，本來就重疊。
- 三個 tier 的**成員資格**才是有證據的部分，**層級之內的名次沒有證據**（2026-07-25 實測：前 3 名 vs 第 4~8 名 t=−0.22，第 2 名反而最差）。畫面列出清單時必須同時寫「照候選清單原始順序，不是好壞排名」。

- [ ] **Step 1: 實作。**
- [ ] **Step 2: `npm run build` 0 error。**
- [ ] **Step 3: 390 寬實跑截圖驗收**（同 Task 7 Step 4 的方法）。
- [ ] **Step 4: Commit** — `feat(ui): 候選情報清單與條件層級分佈卡`

---

### Task 9: 文案與文件同步

**Files:** `src/components/HelpGuide.jsx`、`STATUS.md`、`CLAUDE.md`、`pipeline/opportunities.py` 註解

**🔴 追加（2026-07-26 Andy 在頻道問「所以我該怎樣使用跟看這個標的建議」）：**
他會問，就代表網站沒回答這件事。`HelpGuide.jsx` 第 1 章要改寫成「**你每天實際的動作**」，逐步寫：
1. 晚上收到快報 → 先看市況那一行
2. 看兩份榜的差別（勝率偏優＝比較常賺；報酬偏優＝賺的時候賺比較多；雙優只列一次）
3. **隔天開盤買**——這是回測的口徑，盤中追高或拖到第三天買，網站上的數字就跟你無關了
4. 放 20 個交易日再檢視；中途會先虧是正常的
並且要寫進這三句判斷（都有實測支撐）：
- 「挑對股票」的效果（52% vs 44%）遠小於「多買幾檔」的效果（拉到約 66%）
- 買 1~3 檔不是買到精華，是同一批賭注押更集中（賺錢機率 56~60%、最慘 −44%）
- 目前**沒有**經過驗證的停損建議，第三段才會有——在那之前不要假裝有

- [ ] **Step 1: grep 殘留** — `grep -rn "Top5\|Top 5\|Top8\|Top 8\|買幾檔\|購買組合\|建議買" src/ pipeline/ STATUS.md CLAUDE.md docs/`
- [ ] **Step 2: 逐條改掉；文案一律不寫死檔數。**
- [ ] **Step 3: `python3 -m pytest pipeline/tests -q` + `npm run build` 都綠。**
- [ ] **Step 4: Commit** — `docs: 全站文案改成候選情報語意`

---

### Task 10: 部署與線上驗收

- [ ] **Step 1: `git pull --rebase`**（CI 會把歷史檔 commit 回 main）。
- [ ] **Step 2: `gh workflow run daily.yml -f force_recompute=true`** — ⚠️ **絕不本機 `npm run deploy`**（本機 `public/` 是 gitignored 且過期，會讓線上資料倒退）。
- [ ] **Step 3: 等 CI 綠後，確認 GitHub Pages 真的換版**（CI 綠 ≠ 線上換版，07-25 踩過 Page build failed）：比對線上 HTML 引用的 JS 檔名有無改變。
- [ ] **Step 4: 線上實跑** — `?nocache=<時間戳>` + `ignoreCache`，390 寬截圖。
- [ ] **Step 5: 發 Discord 驗收報告**（含「改口徑前 vs 後」對照表）。

---

## 驗收總表（Andy 看得懂的版本）

| # | 驗收條件 | 怎麼證明 |
|---|---|---|
| 1 | 所有績效數字都是「隔日開盤買」算出來的 | `_reports/口徑更正_隔日開盤_2026-07-26.md` 的前後對照 |
| 2 | 所有勝率都扣了 0.785% 成本 | `tier_stats.json` 有 `cost_pct`；成績單測試斷言 +0.5% 不算贏 |
| 3 | 全站沒有「買幾檔／配置金額／目標價／出場價」 | Task 9 的 grep 結果為空 + 線上截圖 |
| 4 | 榜單不固定檔數 | `opportunities.json` 的 `picks` 長度 ≠ 固定值，且有 `pool_n` |
| 5 | 不顯示個股精確勝率 | 前端只讀 `tier_stats.json` 的層級數字 |
| 6 | 每個統計旁都有獨立批次數與信賴區間 | 線上截圖 |
| 7 | 測試全綠 | `pytest pipeline/tests -q` 輸出 |
