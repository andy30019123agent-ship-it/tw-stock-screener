tokens used
210,704
## 🔴 必修

1. **`collect_events` 與 `exit_analysis` 的持有期差 1 個交易日，確定是 bug。**  
   `[pipeline/backtest_signals.py:427]` 的 `collect_events` 是訊號日 `i`、隔日開盤 `i+1` 進場、`i+forward` 收盤出場；forward=20 時涵蓋 `i+1...i+20` 共 20 個交易日。  
   `[pipeline/backtest_signals.py:1117]` 的 `exit_analysis` 卻從 `entry_i=i+1` 出發，再用 `[pipeline/backtest_signals.py:1131]` 的 `entry_i+h`，h20 實際出場是 `i+21`，共 21 個交易日。它又在 `[pipeline/backtest_signals.py:1050]`／`:1170` 宣稱 h20 是現行 control，因此不是刻意定義差異。若以目前全站口徑為準，應改成 `entry_i+h-1`。

2. **出場分析沒有完整排除價格斷點，h20/h40/trail 都可能出現假報酬。**  
   `[pipeline/backtest_signals.py:435]` 只替事件排除到 `i+20` 的斷點；但目前 h20 會走到 `i+21`，h40/trail 會走到 `i+41`。`[pipeline/backtest_signals.py:1093]` 的全市場 benchmark 更完全沒套斷點過濾。分割／減資落在後半段時，`signal_exits.json` 仍可能混入數十至數百%的假漲跌。

3. **歷史 tier 母體和今日分類母體不是同一個選股程序，現在的「勝率偏優／報酬偏優／雙優」證據不能直接套到候選。**  
   `[pipeline/tier_stats.py:86]` 對所有 `collect_events` 事件分層；事件只要任一 raw signal 成立就會進來，包含 display-only 訊號，見 `[pipeline/backtest_signals.py:467]`。  
   今日候選則另經引擎訊號、500 張流動性、RS gate、營收過濾，見 `[pipeline/opportunities.py:103]`、`:117`、`:127`。今天的百分位是在這個最終候選池算，見 `[pipeline/opportunities.py:190]`。  
   因此「所有訊號事件的高三分位」不等於「今日候選池前 1/3」。但前端直接宣稱這是分類依據，見 `[src/components/Opportunities.jsx:21]`、`[src/components/OutcomeShape.jsx:23]`。`measure_both_tier.py:39-59` 的雙優驗證也沿用錯的母體。必須用歷史資料逐日重播完整 `build_opportunities` 流程後再量。

4. **大量並列值會讓三分位嚴重失衡。**  
   `[pipeline/tier_stats.py:98]` 算兩個門檻後，以 `[pipeline/tier_stats.py:102]` 的 `v <= lo_thresh` 優先歸 low；若 lo==hi，例如 9 筆 `high20_gap` 全為 0，結果是 low=9、mid=0、high=0。我已用純函式重現。  
   這與檔頭「每層至少約 3 筆」的假設相反。需要明確決定：以排名硬切近似等量、相同值保持同層但標記該日不可分層，或增加 tied/unclassified；不能保留現在的隱性全塞 low。

5. **有效但空的 tier 結果會讓前端候選整區報錯。**  
   空層仍會輸出含 `win_rate:null`、`avg_win_pct:null`、`ci90:null` 的物件，見 `[pipeline/tier_stats.py:120]`。  
   `[src/components/OutcomeShape.jsx:70]`、`:73`、`:76` 直接運算 `null`／索引 `ci90[0]`；`[src/components/Opportunities.jsx:54]`、`:65`、`:69` 也一樣。JSON 抓不到時已有降級，但「JSON 存在、某層零樣本或缺欄位」會 crash，由 App 的 ErrorBoundary 把整個 Opportunities 區替換成錯誤卡。必須逐欄做 finite/null guard。

6. **小候選池分類確實會退化成荒謬結果。**  
   `[pipeline/opportunities.py:65]` 規定只有一檔時所有特徵百分位都是 1.0，連 `None` 也不例外；分類規則 `[pipeline/opportunities.py:198]` 因此會把「唯一一檔、bias/RS/前高全部缺值」標成 `both`。我已重現四個 feat_pct 都為 1.0、tier=`both`。  
   n=2 時只要數值不同就是 0/1，也會把單一相對較高者包裝成前段。應設最小分類池、缺值永不達標，並在小池時回 `tier=None` 或明確標示樣本不足。

7. **`tier_stats` 的 broad `except Exception` 會吞掉實作錯誤，工作流仍顯示成功。**  
   `[pipeline/backtest_signals.py:1436]` 到 `:1449` 把 import、計算、schema、寫檔和列印全部包在同一個 catch。它只在 stdout 警告，沒有 machine-readable failure/status。若錯在 `open(...,"w")` 後，舊檔可能已被截斷，訊息卻仍說「沿用既有檔」。  
   `[pipeline/opportunities.py:387]` 載不到新檔就直接略過，也不刪除舊的 public JSON，前端可能繼續顯示過期資料。至少應只 catch 預期的展示性錯誤、採 atomic write，並輸出 `tier_stats_status/stale`；程式錯誤應讓 CI 失敗。

8. **購買組合語意沒有真正拆乾淨。**  
   `[src/components/Opportunities.jsx:357]` 把所有三類候選丟進 `PositionPlan`，`[src/components/PositionPlan.jsx:28]` 到 `:35` 隨即把它們描述成「名義上分散 N 檔／實際獨立賭注」。這是在把候選清單當成已買組合。  
   `[src/components/ConditionPanel.jsx:239]` 和 `[src/components/HelpGuide.jsx:98]`、`:165` 也仍明講「必須分散買足夠多檔／只買一兩檔」。這與「買哪幾檔由使用者決定」直接衝突。

9. **成績單開始混合兩個完全不同的策略版本，且歷史檔會快速膨脹。**  
   `[pipeline/opportunities.py:270]` 保留舊 Top5 歷史，`:275` 起改存每日全部候選，再由 `[pipeline/opportunities.py:314]` 全部混成一個 win rate。未來成績單會把「舊 Top5」和「新全候選」視為同一策略，且候選多的日期權重更大。應加 `spec_version` 並分段統計。  
   同時 `[pipeline/opportunities.py:52]` 還假設每天 5 檔；實際 repo 已從每日至多 5 檔跳到單日 142 檔，檔案由基準 commit 約 6 KB 變成 57 KB。照目前欄位量，十年可能超過百 MB，還會每天製造 Git 歷史膨脹。

10. **使用者文案仍同時宣稱兩種錯誤進場價。**  
    `[src/components/HelpGuide.jsx:302]` 說回測用「訊號當天收盤」；`[src/components/ConditionPanel.jsx:306]` 說出場分析用「訊號次日收盤」。實際程式已是隔日開盤。這不是執行路徑漏改，但會讓使用者以為新口徑沒上線。

## 🟡 建議

1. **moving-block bootstrap 主體合理，但湊長度與邊界處理不精確。**  
   `[pipeline/tier_stats.py:165]` 到 `:175` 抽 `ceil(n_days/block)` 個完整區塊，沒有截回原日數。現有輸出每層 395 天，bootstrap 每次其實使用 400 天。非循環區塊又使序列頭尾日被抽中的機率遠低於中間日，若近期 regime 不同，bootstrap 分布會偏移。建議 circular MBB，拼接後截成恰好 `n_days`。  
   `[pipeline/tier_stats.py:181]` 的第 5／95 百分位取法本身是正確的 90% percentile CI；400 次可用但偏粗。

2. **CI 沒涵蓋「看完同一段資料才挑特徵／命名分類」的不確定性。**  
   `[src/components/OutcomeShape.jsx:18]` 自己也承認尚未做同日配對／walk-forward，但 `[src/components/Opportunities.jsx:19]` 已把雙優寫成「最有根據」。目前 CI 只條件於已選好的特徵與切法，不能處理資料探勘及模型選擇偏誤。分類正式掛「偏優」前應做時間切分或 walk-forward。

3. **全站成本公式仍不是同一把尺。**  
   `[pipeline/backtest_signals.py:954]` 使用乘除式精確成本；`[pipeline/tier_stats.py:108]` 與 `[pipeline/opportunities.py:326]` 則直接減 0.785%。毛報酬 +10% 時，兩者差約 0.076 個百分點；+16% 時差約 0.123 個百分點。勝負門檻差很小，但平均賺幅和期望值會系統性偏高。應共用 `_apply_cost`。

4. **`collect_events` 的最小長度檢查多要求一根 K。**  
   `[pipeline/backtest_signals.py:406]` 要求 `min_bars + forward + 1`；但 `[pipeline/backtest_signals.py:427]` 的索引在長度剛好 `min_bars + forward` 時已有一筆合法事件。沒有 IndexError，只是少掉邊界股票的一筆樣本。

5. **Telegram 摘要無法和候選總數對帳，數字又寫死。**  
   `[pipeline/notify_tg.py:279]` 到 `:286` 只列 three tiers，不交代 `tier=None` 的數量。目前 public 資料是 132 檔中 77 檔未分類，讀者只看到 55 檔的分類段落。另 `[pipeline/notify_tg.py:174]` 把 49%、+16%、−10% 寫死，下一次每週重算後可能與網站 JSON 不一致。應顯示「其他未分類 N 檔」，並讀 `tier_stats.json` 或避免精確數字。

6. **績效檢視日漏掉台股休市日。**  
   `[src/lib/position.js:63]` 只跳週末；`[src/components/PositionPlan.jsx:66]` 卻顯示成確切日期而不是「約」。遇國定假日會早一至數天。

7. **新驗證腳本不可攜。**  
   `[pipeline/measure_both_tier.py:16]`、`:17`、`:84` 寫死使用者絕對路徑；CI、其他開發者或 repo 搬家後直接失效。應以 `__file__` 推導路徑。

## ✅ 檢查過沒問題

1. **主要進場價改動接線正確。**  
   `[pipeline/backtest_signals.py:432]` 用 `adj_open[i+1]`，`:448-449` 同一個 ret 同時進 event 與 `by_date`；沒有發現其他可執行績效路徑仍以訊號當日收盤進場。  
   `[pipeline/opportunities.py:319]` 到 `:326` 的 scoreboard 也使用隔日開盤及 `pick+forward` 收盤。

2. **`exit_analysis` 內部的進場價與 benchmark 都已改成開盤。**  
   `[pipeline/backtest_signals.py:1096]` benchmark 用當日 open，`:1120` 事件也用次日 open；除了上述持有期 off-by-one／斷點問題，兩者內部口徑一致。

3. **正常資料下沒有 IndexError。**  
   `collect_events` 的 range 保證 `i+forward` 和 `i+1` 合法；`[pipeline/backtest_signals.py:1118]` 正確保護 40 日路徑；scoreboard 的 `[pipeline/opportunities.py:317]` 也先檢查日曆邊界。

4. **point-in-time 特徵來源正確。**  
   `[pipeline/backtest_signals.py:468]` 到 `:480` 全部取訊號當下的 `ind`／`xsect`，沒有回頭讀最新值。turnover 用當時均量×當時收盤，也避開歷史成交金額恆為 0 的問題。

5. **`feat_pct` 分母確實是最終當日候選池。**  
   `[pipeline/opportunities.py:125]` 到 `:188` 先完成 gate、訊號、流動性及營收過濾，`:193` 才對 `picks/feat_raw` 排百分位。不是全市場、也不是 gate 前母體。

6. **前端基本降級路徑存在。**  
   tier JSON 抓不到會在 `[src/App.jsx:84]` 變成 null；picks 空在 `[src/components/Opportunities.jsx:262]` 有正常畫面；`feat_pct` 缺欄位由 `[src/components/Opportunities.jsx:79]` 的空物件與 `:113` 的 null guard 處理。需補的是前述「tier 物件存在但統計欄位為 null」。

7. **bootstrap 的核心方向正確。**  
   `[pipeline/tier_stats.py:157]` 先按日期群聚，抽樣時保留同日全部事件；固定獨立 RNG，結果可重現。這比逐事件 bootstrap 合理。

8. **驗證狀態。**  
   Python 重點測試 84 項通過；12 項 Telegram 測試因唯讀 sandbox 無法建立 `tmp_path` 而停在 setup，沒有 assertion failure。`position.js` 23 項斷言全過；四個改動 JSX 均通過 Vite/esbuild 語法解析。工作樹未修改。
