# 清查：死碼與專案衛生（2026-07-25）

範圍：`tw-stock-screener`（React+Vite `src/` ＋ Python 管線 `pipeline/`）。唯讀清查，未改動／未刪除任何檔案。
測試基準：`python3 -m pytest pipeline/tests -q` → 144 passed（清查前後皆已跑過，無變動）。

---

## 一、死碼（附證據）

### 1.1 `pipeline/build_data.py:29` `sma()` 函式 — 🟢建議刪
```python
def sma(vals, n):
    if len(vals) < n:
        return None
    return sum(vals[-n:]) / n
```
- 證據：`grep -rn "\bsma(" pipeline/ src/ pipeline/tests/` 只命中定義那一行本身，全專案（含測試）零呼叫。
- 已被 `sma_series()`（同檔 34 行）取代——後者在 189~192 行被實際呼叫（算 ma5/ma10/ma20/ma60）。
- 風險：低。純函式、無副作用，刪除不影響任何輸出。

### 1.2 `pipeline/backtest_signals.py:490` `run_backtest()` 函式 — 🟡建議刪，但先確認非對外 API
```python
def run_backtest(price_hist, chip_hist, div_hist, universe, forward=FORWARD, min_bars=MIN_BARS):
    """對全市場回測訊號，回傳 stats_to_weights 的輸出。"""
    events, by_date = collect_events(price_hist, chip_hist, div_hist, universe, forward, min_bars)
    return stats_to_weights(events_to_stats(events, by_date))
```
- 證據：`grep -rn "run_backtest" pipeline/ .github/` 只命中定義本身，零呼叫；`pipeline/tests/` 也未 import。
- 實際生產路徑是 `ensure_weights()`（1240 行，CLI `__main__` 呼叫的就是它）——它不呼叫 `run_backtest()`，而是自己內嵌了一套更完整的流程（長歷史合併、價格斷點偵測、橫斷面 cache、除權息覆蓋率警告後才 `collect_events`）。`run_backtest()` 看起來是早期較簡化的版本，被 `ensure_weights()` 吸收取代後沒清掉。
- 🟡 風險提示：不像 `sma()` 那麼確定無害——`run_backtest` 簽名乾淨（吃 4 個直接參數、無副作用），有可能是刻意留給未來單元測試或外部腳本呼叫用的簡化入口。建議刪前搜一下是否有計畫要用它做單測，而不是直接砍。

### 1.3 `pipeline/market_sources.py:163` `fetch_listed_ohlc_month()` 函式 — 🟢建議刪
```python
def fetch_listed_ohlc_month(sid, yyyymm):
    """單一上市股某月各日 OHLCV（STOCK_DAY，吃 date；回填上市歷史用）。回 list[row]。"""
```
- 證據：`grep -rn "fetch_listed_ohlc_month" pipeline/` 只命中定義本身。docstring 自稱「回填上市歷史用」，但 `pipeline/backfill.py` 實際的回填邏輯（`backfill_price_by_day`，50 行）呼叫的是逐日版 `ms.fetch_listed_ohlc(day)`，不是這支逐月版。
- 判斷：舊的「逐月抓」策略被「逐日抓」取代後留下的孤兒函式。
- 風險：低，純讀取函式無副作用。

### 1.4 CSS：`.sort-bar` / `.sort-bar-label` / `.sort-chip` / `.sort-chip.on` — 🟢建議刪
- 位置：`src/App.css:709-718`（`.sort-bar, .card-list { display: none; }` 這行裡 `.card-list` 仍在用，`.sort-bar` 不在用；711~718 行的 `.sort-bar`／`.sort-bar-label`／`.sort-chip`／`.sort-chip.on` 四條規則整組都是孤兒）。
- 證據：`grep -rin "sort-bar\|sortbar\|sort-chip\|sortchip" src/*.jsx src/components/*.jsx` 零命中（含大小寫、含 class 組字串都查過，排除了動態組 class 名的可能）。
- 直接證據：`src/App.jsx` 226 行附近的註解自己承認被取代——「排序選單：底部彈出，固定二欄格線（取代原本清單前參差的 10 顆排序 pill）」，新版用的是 `.sort-sheet` / `.sort-opt`（App.jsx 230~238 行，這組有在用）。`.sort-bar` 系列就是那個「原本的排序 pill」遺留的死 CSS。

### 1.5 已核對、確認不是死碼的候選（記錄以免日後重查）
以下是初篩時看起來像「未被引用」，但深查後確認是**動態組字串**（`` `prefix-${var}` ``），不能刪：
- `oos-muted` / `oos-robust` / `oos-warn`（`src/lib/oos.js` 常數 → `` `oos-badge ${ob.cls}` `` 組出）
- `combo-rel-weak` / `combo-rel-unknown`（`src/components/ComboBoard.jsx:141` `` `combo-rel-${rel.overall}` ``）
- `wq-up` / `wq-down` / `wq-flat`（`src/components/Opportunities.jsx:228` `` `wq-trend wq-${t}` ``）
- `tone-down`（`src/components/MarketAdvice.jsx:46` `` `market-advice tone-${st.tone}` ``）
- `_curl` / `_urllib`（`pipeline/market_sources.py:49` 以 `for fn in (_curl, _urllib):` 存進 tuple 再呼叫，不是字面呼叫）

### 1.6 測試檔
`pipeline/tests/*.py` 未發現 `@pytest.mark.skip`、`xfail`、`assert True` 之類的死測試（全文 grep 零命中）。

---

## 二、註解與程式不一致（最需要優先看）

### 2.1 🔴 `src/components/ConditionPanel.jsx:78-79` 與 226 行 — 過時，且與同檔案 391 行自相矛盾
- 78-79 行：「Phase B 橫斷面新訊號（相對強弱＋產業輪動）——**只做回測展示、不進 Top5 選股、不做即時篩選**」
- 226 行：「Phase B：相對強弱＋產業輪動新訊號的樣本外驗證（研究展示，**不進 Top5、不做即時篩選**）」
- 事實（同一份檔案 391 行的註解自己講清楚了）：「橫斷面相對強弱／產業輪動（**2026-07-25 從「僅戰績」升級為可篩選**）」，往下 400-406 行就是三個真的會篩選的 checkbox（`rsConfirmed`／`rsStrong60`／`industryHot`）。
- 「不做即時篩選」＝假：`src/lib/filters.js:60-62` 對 `s.rs_strong_60`／`s.rs_confirmed_60_120`／`s.industry_hot` 做真的布林過濾，就是即時篩選本身。
- 「不進 Top5」＝過時：`pipeline/opportunities.py:39` 把 `rs_confirmed_60_120` 設成 `GATE_SIGNAL`（強勢雙確認），作為 Top5 候選的**入場門檻**（39-40、73-79 行），不是「只做展示」。（`bull_aligned`／`fill_fast` 那句「不進 Top5」倒是仍然正確，見下方 2.2。）
- 建議動作：改。把 78-79、226 行的說明改成「`rs_confirmed_60_120` 已升級為 Top5 入場門檻＋即時篩選；`rs_strong_60`／`industry_hot` 仍只做展示＋即時篩選（未進 Top5 計分）」這類精確描述，並刪掉跟 391 行矛盾的舊句子。

### 2.2 對照確認：`ConditionPanel.jsx:214`「多頭排列／填息快僅供戰績參考、不進 Top5 選股」— 仍正確
- `pipeline/opportunities.py:61-64`：`eligible()` 只取 `bt.ENGINE_SIGNALS` 內的訊號計分，`bull_aligned`／`fill_fast` 不在 `ENGINE_SIGNALS` 列表裡（`pipeline/backtest_signals.py:51` 起、108-110 行註解也重申「展示型（不進引擎）：多頭排列＝MA5>10>20>60；填息快＝已填息且天數 ≤10」）。此句與程式碼一致，不用改。

### 2.3 🟡 `STATUS.md:10-11` 與 root `CLAUDE.md` 專案描述 — 選股宇宙範圍已過時
- `STATUS.md`：「資料：FinMind 一檔一檔抓（**限有量電子股，約 111 檔主流**），千張大戶走集保 TDCC 免費週更。」
- 事實：`pipeline/build_universe.py` 現在產的是「**全市場**」清單（62、108 行印出的字面就是「全市場」），`pipeline/universe.json` 實測 **1966 檔**、橫跨電子零組件/半導體/生技醫療/建材營造/紡織/鋼鐵/觀光餐旅等全部產業，不再限電子股。
- 連帶：專案根目錄 `CLAUDE.md` 開頭也寫「台股**電子股**選股網頁」，跟現在全市場的實際範圍不一致——這份是會被未來 session 讀到的專案指令檔，範圍描述錯會誤導後續開發判斷（例如誤以為新股票不會出現在候選池）。
- 建議動作：`STATUS.md` 本身已過期一個多月（最後更新 2026-06-20，git log 顯示最後改動 `Sat Jun 20`），且與 CLAUDE.md 指定的「現況見記憶檔 `project_tw_stock_screener.md`」重複、容易兩邊對不上——建議整份改成短導引（「現況見記憶檔，勿手動維護本檔案細節」）或直接砍掉這份，避免有兩份會漂移的現況說明。CLAUDE.md 的「電子股」字樣建議一併修正為實際範圍描述。

### 2.4 已核對、確認註解與程式一致（記錄以免日後重查）
- `pipeline/history_store.py:14` `PRICE_WINDOW = 110` 與各處註解「滾動 110 天」數字一致。
- `pipeline/notify_tg.py:71-96` `score()` 的 docstring（「改成讀回測權重、不再手寫 +2/+3」）與實際實作一致，且與根目錄 `eval_notify_threshold.py` 描述的背景吻合。
- `pipeline/opportunities.py` 開頭的過濾規則清單（近月營收 YoY<0 剔除、20 日均量<500 剔除、乖離>15% 標旗標）與下方 33-96 行實作逐條核對一致。

---

## 三、過期檔案與專案衛生

### 3.1 🔴 `_audit_screenshots/` 20 個 PNG 已進版控，未被 `.gitignore` 蓋到
- 證據：`git ls-files _audit_screenshots/ | wc -l` → 20；`git ls-files -s | sort -k4` 顯示其中最大到 1.5MB（`audit_1280_full.png`），整目錄合計約 9MB。
- `.gitignore` 只有 `/*.png`（只擋根目錄），不擋子目錄，所以 `_audit_screenshots/*.png` 全部進了 repo 歷史，而且還在持續累積（`git status --short` 目前另有 2 張未加入的新截圖 `375_home.png`／`375_top5expanded.png`）。
- 建議動作：把 `_audit_screenshots/` 整個加進 `.gitignore`（跟根目錄驗證截圖同一套邏輯：拋棄式證據不進版控），並評估要不要用 `git rm -r --cached` 把已進歷史的 9MB 清掉（會改變檔案在 repo 裡的存在，需要你確認要不要動手，此清查不代做）。

### 3.2 🟡 root 兩支一次性驗證腳本 `eval_notify_threshold.py`／`verify_outlier_dependence.py`
- 皆是 2026-07-25（今天）為了校準/覆核而寫的唯讀分析腳本，不進 CI、不被任何模組 import（`grep -rln` 全專案零命中）。內容跟今天的審查工作直接相關，刪除可能之後想重跑就要重寫。
- 建議動作：不是死碼（近期、有明確用途），但擺在專案根目錄會跟核心程式混在一起——建議移到 `pipeline/scripts/` 或 `_reports/` 之類的資料夾，或至少在下次確認校準結論後歸檔/刪除。此為衛生建議，非誤刪風險項。

### 3.3 確認沒問題的部分（順帶記錄，避免重複排查）
- `public/data/` 服務用副本：目前實際存在的 8 個頂層檔案（`screener.json`／`opportunities.json`／`signal_weights.json`／`scoreboard.json`／`signal_windows.json`／`signal_regime.json`／`signal_combos.json`／`signal_exits.json`）＋ `charts/` 目錄，跟 `.gitignore` 裡列的 9 條規則逐一對上，`git ls-files public/data/` 為 0——沒有漏蓋的情況。
- `pipeline/` 持久化副本（`signal_weights.json`／`scoreboard.json`／`universe.json`／`picks_history.json`／`revenue_cache.json`／`holders_history.json` 等）皆確認有進版控，符合設計意圖。
- `小詩/`、根目錄 `盤點報告_2026-06-21.md`／`盤點報告_2026-06-28.md`、根目錄零散 `.png`（`verify_390.png` 等）：`git status --ignored` 確認皆已正確被忽略，只是本機殘留檔，不是版控問題。
- `package.json` 4 個 dependencies（`lightweight-charts`／`lucide-react`／`react`／`react-dom`）全部在 `src/` 有實際 import，無用不到的依賴。
- Python／JS 訊號門檻未發現重複維護：`MIN_VOL_LOTS`／`BIAS_FLAG_PCT`／`RS_CONFIRM_120_PCT` 等數值只在 Python 端定義，前端只讀後端算好的布林旗標，沒有漂移風險。
- 大檔（`pipeline/history/bt_price.json` 18MB、`price.json` 11MB）屬於刻意持久化的長歷史資料，非誤存。

---

## 四、重複與可合併

### 4.1 未發現需要合併的重複邏輯
- 對 `pipeline/*.py` 做了函式名稱去重掃描（`grep -rn "^def "` 全專案比對），除了各 CLI 腳本各自的 `main()`（10 個，各自獨立入口，非重複）外，沒有發現同名或明顯重工的業務邏輯函式。
- 前後端未發現重複實作同一個判定邏輯（見 3.3 最後一項）。
- 前端元件間也沒抓到重複的格式化/計算函式（`ResultTable.jsx`／`Opportunities.jsx`／`ComboBoard.jsx` 各自的格式化邏輯依賴的都是共用的 `src/lib/oos.js`／`src/lib/filters.js`，沒有各寫一份）。

本節無新增建議項目。
