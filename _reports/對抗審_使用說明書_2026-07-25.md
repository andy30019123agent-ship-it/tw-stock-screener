# 對抗審查：使用說明書（#help）— 2026-07-25

審查對象：`src/components/HelpGuide.jsx`（新增）、`src/App.jsx`（hash 路由＋入口）、`src/App.css:921-1022`（說明書段 CSS）
審查方式：逐句對 `pipeline/*.py`／`pipeline/*.json`／`dist/data/*.json` 核算 ＋ headless Chrome CDP 實測 375／768／1280（`?v=review#help`，`Network.setCacheDisabled` ＋ `ignoreCache` reload）
立場：唯讀，未改任何檔案。
⚠️ 注意：審查中期發現 `HelpGuide.jsx` 在我第一次讀檔後又被改過（s2「約150檔/約20檔」→「140~170檔/十幾到二十幾檔」、s3「2~3%」→「2.3%~4.5%」）。**本報告全部以 18:22:42 版本（現行檔案＋現行 dist bundle）為準**，已重新核對。

結論：**有 17 個問題**（🔴2／🟡8／🟢7），其中 2 個是「說明書內容與程式碼實際行為不一致」。

---

## 🔴 高（會直接誤導投資決策）

### 🔴1. 開頭宣稱「每個數字都已扣掉手續費、證交稅與大盤漲幅」——市況建議卡／市況分層表／勝率榜是毛值
- 位置：`src/components/HelpGuide.jsx:42`（副標）、`src/components/HelpGuide.jsx:63`（s1 叫 Andy 每天必看這張卡）
- 反證：
  - `src/components/MarketAdvice.jsx:58` → `+{x.cur.avg_excess}pp`；`pipeline/signal_regime.json` 每個 cell 只有 `avg_excess`（毛），沒有任何成本後欄位。
  - `src/components/ConditionPanel.jsx:92-93`（市況分層表 `regimeCell` 用 `cell.avg_excess`）、`pipeline/signal_windows.json`（多時間窗勝率榜也只有 `avg_excess`）。
  - 實測 375 寬畫面（本機 dist）市況建議卡顯示：`強勢雙確認 +2.34pp／強勢股60日 +1.56pp／縮口帶量突破 +1.25pp／多頭排列 +1.24pp／爆量突破 +1.06pp`，全部標「全天候」。其中 **爆量突破的全樣本成本後超額是 −0.06pp**（`pipeline/signal_weights.json` → `signal_breakout.quality.net_excess_expectancy = -0.06`）。來回成本 0.785pp（`signal_exits.json.cost.round_trip_pct`），所以毛值 <0.785 一律是實際淨虧。
  - 另一處同病：機會股成績單「平均報酬」＝純絕對報酬，未扣成本也未扣大盤（`pipeline/opportunities.py:196-205`）。目前 `dist/data/scoreboard.json` samples=0 還沒顯示，一有樣本就會出現在畫面上。
- 為什麼是問題：這正是 07-25 一整天在修的那個錯（「畫面數字不是決策依據」）。說明書用一句話把毛值蓋章成淨值，Andy 會拿 +1.06pp 當「贏大盤 1 個百分點」去空頭加碼，實際是輸的。
- 修法（二選一）：①副標改成「Top5／各策略戰績／組合排行榜的數字已扣成本；**市況建議卡、市況分層表、多時間窗勝率榜仍是毛超額，要自己再扣 0.785pp**」，並在 s1 第 2 點補這句；②後端 `build_regime_series`／`signal_windows` 補 `net_excess` 欄位，前端改吃淨值，副標才留得住。

### 🔴2. 「相對強弱（RS）」定義寫成百分位，但畫面上標 RS 的地方是「個股 − 加權指數」
- 位置：`src/components/HelpGuide.jsx:138-141`（「漲幅在全市場排第幾（百分位）。0.9 代表贏過 90% 的股票。**注意這是跟全市場其他股票比排名，不是跟大盤指數比**」）
- 反證：
  - `pipeline/build_data.py:181` `rs20 = round(stock_ret20 - index_ret20, 2)`；`build_data.py:411` 註解「相對強弱：個股近20日報酬 － 加權指數近20日報酬（百分點）」。
  - 顯示處：`src/components/Opportunities.jsx:149-151`（`RS {p.rs20}`）、`src/components/ResultTable.jsx:7-13`（欄位名「相對強弱」，`RS {s.rs20}%`）、排序 `src/lib/filters.js:173`。
  - 實測畫面：機會股卡片 `RS +75.34`、表格 `RS +21.5%`。
  - 真正的百分位是 `rs_pct_60/rs_pct_120`（只用來判強勢雙確認），UI 上另外叫「新訊號研究：相對強弱」（`ConditionPanel.jsx:252` 那句「跟全市場排名（不是跟大盤指數比）」講的是這一個）。
- 為什麼是問題：同一頁的 s2 第 4 層「按近 20 天相對強弱排序」用的是 rs20（對指數），s4 的 Term 卻否認「跟指數比」。照說明書讀，`RS +21.5%` 會被理解成「只贏過 21.5% 的股票＝很弱」，實際是「20 天贏大盤 21.5 個百分點＝很強」；`RS +75.34` 更完全無法對應 0~1 的百分位。
- 修法：Term 拆兩條——「RS（卡片／表格徽章）＝近 20 交易日報酬減加權指數，單位百分點，+21.5 就是贏大盤 21.5pp」；「全市場百分位（強勢雙確認用的）＝0.9 代表贏過 90% 的股票，只出現在『新訊號研究』區」。

---

## 🟡 中

### 🟡3. s5 的記號圖例與程式判準不符（⚠️／? 的意思都寫錯）
- 位置：`HelpGuide.jsx:156-159`
- 反證：chip 上的記號只由樣本外狀態決定——`src/lib/oos.js` 末段 `TIER_MARK`：`weak→'⚠'`、`unknown→'?'`、`pass→null`，`sigTier()` 只看 `oos.status`，完全不看成本。`ConditionPanel.jsx:132,154`。
  實測 1280 寬 8 顆 chip：`強勢雙確認 +1.39pp（無記號）／多頭排列 +0.25pp（無記號）／**爆量突破 −0.06pp（無記號）**／糾結轉強 −0.64pp「?」／小詩選股 −0.78pp「⚠」／填息快 −0.84pp「?」／同業低估「?」／外資連買「?」`。
- 為什麼是問題：①說明書說「⚠️＝扣成本後贏不過大盤」，但 −0.06pp 的爆量突破沒有任何記號、說明書又說「沒有記號＝通過驗證」→ 兩句合起來會把一個淨虧策略讀成合格；②說明書說「?＝樣本太少」，但糾結轉強的 `?` 實際是 `never_qualified`（樣本 2333 筆一點都不少，是「沒有任何一段訓練期看好過它」）。
- 修法：圖例改成「⚠／?＝樣本外驗證的結果（⚠ 撐不住、? 還無法判斷或沒回測資料）；『扣成本後贏不過大盤』要看展開後戰績表策略名稱旁的 ⚠️，以及按鈕上的數字是不是負的」。

### 🟡4. s2 第 2 層「剩約 140~170 檔」不是「篩強勢雙確認」的檔數
- 位置：`HelpGuide.jsx:78`
- 反證：今日 `dist/data/screener.json` 925 檔裡 `rs_confirmed_60_120 = true` 的有 **197 檔**（我用 screener.json 重算；記憶檔 `project_tw_stock_screener.md:56` 也記 197）。140~170 對應的是 `gate.pool`＝強勢雙確認 **∩ 有引擎訊號 ∩ 均量≥500張 ∩ 營收不年減**（`pipeline/opportunities.py:73`），今日 143（`dist/data/opportunities.json`，畫面 opp-sub 顯示「今日 143 檔」）、上一版 165。
- 為什麼是問題：層與層的檔數是 Andy 判斷「今天篩得嚴不嚴」的依據；把第 3 層的條件偷偷算進第 2 層，會讓他以為強勢股只有 140 檔（實際 197）。
- 修法：第 2 層寫「強勢雙確認（今日 197 檔）」，並補一句「卡片上顯示的『今日 N 檔』是再加上技術訊號＋量能＋營收之後的數（今日 143），以畫面為準」。

### 🟡5. 第 3、4 層把「縮口帶量突破」寫成硬篩選，程式其實是加權排序；漏講「無加權訊號」那種日子
- 位置：`HelpGuide.jsx:82,86`
- 反證：`pipeline/opportunities.py:63-64` pool＝「任一引擎訊號＋量能」；`:101` `score = Σ 權重`（引擎訊號中只有 `sn_squeeze_breakout` 權重 1）；`:125` `picks.sort(key=(-score, -rs20))`。所以縮口不是 filter，是排序分數；`:106-110` 明確保留「一個加權訊號都沒有」的分支，理由會顯示「僅符合門檻（強勢雙確認），無加權訊號、按相對強弱排序」。
- 為什麼是問題：今日 `強勢雙確認 ∩ 縮口 = 18 檔`（我重算，與說明書的 18 一致），剛好 >5 所以描述碰巧成立；縮口候選 <5 檔的日子，Top5 會補進 score 0 的股票（可能只有破底翻這種淨 −1.8pp 的訊號），Andy 會看到說明書裡完全沒出現過的理由文案，且以為每檔都有縮口突破。第 4 層也漏了「先比分數、分數同才比 RS」。
- 修法：第 3 層改「有『縮口帶量突破』的排前面（今日 18 檔）」；第 4 層改「先按權重分數、同分才按近 20 天相對強弱，取前 5」；加一句「縮口候選不足 5 檔的日子，會補進只符合門檻的股票，卡片理由會寫『僅符合門檻…』」。

### 🟡6. s7「只存了 20 天的籌碼歷史」＝過期數字
- 位置：`HelpGuide.jsx:202`
- 反證：`pipeline/history/chip.json` 實際有 **39 個交易日**（2026-05-29 ~ 2026-07-24，我用 python 展開日期集合算的）。
- 為什麼是問題：結論（三個籌碼訊號樣本 0）仍成立（`signal_weights.json` foreign_buy／trust_buy／holder_rising samples=0），但數字錯，且 20→39 天的差別會影響 Andy 判斷「還要多久才有戰績」。
- 修法：改「目前只累積約 2 個月（39 個交易日，從 2026-05-29 起）的籌碼歷史，兩年回測期絕大部分沒有籌碼資料 → 樣本 0」。

### 🟡7. s1「由上往下看三個地方…①最上面的橫幅」——多頭（綠）時根本沒有橫幅
- 位置：`HelpGuide.jsx:62`
- 反證：`src/App.jsx:175` 橫幅只在 `['red','severe_red','yellow']` 才渲染；green 不渲染。（今天是 severe_red，所以實測看得到，多頭時會消失。）
- 修法：改「有橫幅時先看它（多頭時不會出現，沒看到＝廣度沒轉弱）」。順帶：`MarketAdvice.jsx:14` 還有第 4 種狀態 `severe_red`（畫面文字「明顯走弱（空頭）」），s4 的「市況（紅／黃／綠）」只寫三檔。

### 🟡8. 新增的說明書入口觸控目標 34px < 44px（MASTER §7 明列「不可交付」）
- 位置：`src/App.css:1008-1012` `.help-link`（`padding: 4px 2px`，inline-flex）
- 反證：CDP 實測 375 寬，hero 入口 `85×34`、footer 入口 `96×34`（`getBoundingClientRect`）。MASTER.md:140「❌ 觸控目標 <44px」。
- 為什麼是問題：這是手機上進說明書的唯一入口（說明書自己在註解裡說「手機第一眼就要看得到，否則等於沒做」），34px 在 iPhone 上容易點不到／誤點到旁邊的副標文字。
- 修法：`.help-link { min-height: 44px; padding: 0 8px; }`（已是 inline-flex，加 min-height 即可，不會撐壞副標行高就再配 `align-items:center`）。

### 🟡9. 內文 15px（.9375rem）：MASTER §7 禁「內文 <16px」、§2 字級表也沒有這一級
- 位置：`src/App.css:973-976`（`.help-why`）、`996-999`（`.help-note`，還帶 `!important`）
- 反證：CDP 實測 375 寬 computed fontSize＝`15px`，套在 `.help-note ×4`（含 s2 那段「⚠️ 門檻未套用」的決策相關說明）與 `.help-why ×3`。MASTER.md:62 只給「內文 1rem／輔助 0.875rem」，139「❌ 內文 <16px」。
- 補充（避免誤報）：全站既有 CSS 已有 17 處 `.9375rem`，這是既有慣例、不是本次首創；但 MASTER 是權威檔，且 `.help-note` 承載的是正文內容不是圖說。
- 修法：`.help-note`／`.help-why` 提到 `1rem`，或明確在 MASTER 補一級 0.9375rem 並限定用途。

### 🟡10. （非本次改動，但屬本次要查的兩個項目：紅綠語意色與裸 hex）
- `src/App.css:140-141` `.banner-error { background: var(--up-bg); color: var(--up); }` → 台股「漲」的紅被用來畫「大盤明顯走弱」的警告：既是把行情語意色用在非漲跌用途，語意還相反（MASTER.md:48「漲跌與 primary 永不同時承擔強調語意」、鐵則是紅綠只承擔行情語意）。說明書 `HelpGuide.jsx:62` 現在把它寫成正式規則（「紅色代表多數股票都在跌」），等於把這個誤用固化成文件。
- `src/App.css:382,383,386` `.market-advice.tone-down` 有裸 hex `#E0A000`／`#FBF0D8`／`#C08800`；`src/App.css:398` `.ma-apply { background: var(--primary); color: #fff; }` → MASTER.md:136「❌ 元件內裸 hex」，且 MASTER.md:32 明訂主鈕文字用 `--on-primary`（深莓），白字對比不足（#fff on #FF4D8D ≈ 2.9:1 < 4.5:1）。
- 說明書自己那段 CSS（921-1022）**沒有**任何裸 hex，也沒用到 `--up/--down`：這部分查過沒問題。

---

## 🟢 低／待確認

11. 🟢 s3「實測 27 萬筆歷史紀錄」對不上任何檔案。最接近的是 `pipeline/signal_exits.json.n_events = 261390`（26.1 萬），但那是「出場分析」的事件數；而同句要支撐的「每個訊號的中位數」來自 `signal_weights.json` 各訊號 `quality`（樣本合計 83,724，且訊號間重疊）。**待確認**：若「27 萬」指回測總事件數，需要 pipeline 重跑時的 stdout／新增欄位當證據；否則建議改寫成「26 萬筆出場模擬 ＋ 8.4 萬筆訊號事件」。
12. 🟢 s6「過關的組合樣本中位數是 3000 筆、跨 300 個交易日」：實際 18 組過關的中位數是 **2801.5 筆 / 332 天**（我用 `signal_combos.json` 算）。3000 偏樂觀、300 偏保守，建議寫 2800／330。
13. 🟢 s2「全市場約 925 檔（有成交量的上市＋上櫃，已排除 ETF）」：925 是「20 日均量 ≥500 張」過濾後的數（`pipeline/build_data.py:633-634,688`），真正母體是 1966 檔（`screener.json.universe_count`）。同段 note 又把「成交量太小」列成後面才做的過濾（其實已含在 925 裡）。建議寫「全市場 1966 檔 → 20 日均量 ≥500 張的 925 檔」。
14. 🟢 s2「2026-07-25 當天是 18 檔」：18 檔我用 `dist/data/screener.json` 重算＝18 ✓，但那份資料的交易日是 **2026-07-24**（`data_date`）。日期標籤建議改 07-24。
15. 🟢 s4「組合排行榜同時比較 120 組」與 `ComboBoard.jsx:75,113` 的「同時比較數百個組合」不一致；Bonferroni 的檢定數是 `len(acc)`＝所有評估過的組合（`backtest_signals.py:582`），遠多於 120。畫面顯示的 18／120 與說明書一致，所以不影響讀數，但描述統計程序時不精確。
16. 🟢 說明書寫的是彩色 emoji「⚠️」（`HelpGuide.jsx:90,157`），chip 上實際是細線字元「⚠」(U+26A0，`lib/oos.js` TIER_MARK)，外觀差很多，對照時可能認不出來。（不算 MASTER 的「emoji 當 icon」違規——它是引用畫面上的記號，不是裝飾。）
17. 🟢 說明書段 CSS 間距有 14 處非 4/8 倍數（10/14/18/22px），嚴格違反 MASTER.md:138；但非 help 段已有 60+ 處同樣寫法，屬全站既有慣例，非新問題。

---

## 逐條驗過、確認沒問題（不是湊數，是明確排除）

**內容／數字**
- 第 2 層門檻定義「近 60 天前 20%、近 120 天前 30%」＝ `RS_STRONG_PCT=0.80`／`RS_CONFIRM_120_PCT=0.70`（`backtest_signals.py:84-85`）✓
- 「不到 20 檔就取消第一關並標『⚠️ 門檻未套用』」＝ `GATE_MIN_POOL=20`（`opportunities.py:40,74`）＋畫面文案（`Opportunities.jsx:108-111`，兩種 reason 都寫「門檻未套用」）✓
- 成本後超額：強勢雙確認 +1.39 / 多頭排列 +0.25 / 小詩選股 −0.78 / 破底翻 −1.8 / 縮口毛 +0.84→淨 +0.04 / 來回 0.785%——全部逐一對上 `signal_weights.json` 的 `quality.net_excess_expectancy` 與 `signal_exits.json.cost.round_trip_pct` ✓
- 「中位數都是輸給大盤 2.3%~4.5%」＝實際 `median_net_excess` 最好 −2.31（仙人指路）、最差 −4.5（填息快）✓（改版後精準）
- 「沒有訊號贏過大盤比例超過 41%，最低只有 33%」＝實際最高 40.73%（強勢雙確認）、最低 33.23%（破底翻）✓
- 組合榜：120 組、18 組過關、通過檢定第一名「熱門產業＋強勢雙確認＋縮口帶量突破」成本後 +3.77／樣本 1384／跨 287 天——全部對上 `signal_combos.json`，且 `avg_excess` 確實是成本後（毛值另存 `gross_excess=4.61`）✓
- s5「目前只有強勢雙確認 +1.39 與多頭排列 +0.25 是正的且通過驗證」——實測畫面 8 顆快速套用 chip 完全相符（其餘 6 顆都是負值或無回測）✓
- 「相對強弱／產業輪動 2026-07-25 起可篩選」＝ `lib/filters.js:60-62` ＋ `ConditionPanel.jsx:402-406` 確實有 checkbox ✓（附帶：`ConditionPanel.jsx:78` 與 `MarketAdvice.jsx:61` 還留著「不做即時篩選／僅戰績」的舊註解與 tooltip，是程式端的陳舊字串，不是說明書的錯）
- 「回測歷史只有兩年（2024-08 起）」＝ `pipeline/history/bt_price.json` dates 479 個、2024-08-01 ~ 2026-07-24 ✓
- 「OOS 只累積到 120 個決策日、約 3 個月後達標」＝ 6 折 × test 20 天＝120（`signal_weights.json.phase_a`、`walk_forward_oos` 每 20 天多一折）✓。「專案自己訂的標準是 180 天」只寫在記憶檔（`project_tw_stock_screener.md:78,99`），程式與 docs 裡沒有這個常數——不算錯，但建議把門檻寫進 spec 免得日後對不上。
- 「持有 40 天成本後表現更好」＝ h40 `avg_net_excess +0.13` > h20 `−0.25`，且 trail10 的絕對報酬 1.51 < h40 的 4.5 ✓
- 「籌碼三訊號樣本 0」✓、「已排除 ETF」＝ `build_universe.py:55` ✓、「乖離＝離 20 日均線多遠」✓（`BIAS_FLAG_PCT=15`）、「縮口帶量突破＝帶寬壓到近 60 日第 30 百分位後帶量收上上軌」✓（`build_data.py:306-330`）
- 「進場價用訊號當天收盤、不含停損停利、固定持有 20 交易日」✓（`FORWARD=20`、`backtest_signals.py:1025`）

**anti-slop（說明書段）**：emoji 當 icon ✗無（icon 全走 lucide `ArrowLeft/BookOpen`）／隨機漸層 ✗無／三卡並排萬用版型 ✗無／裸 hex ✗無（全 `var(--token)`，`--warn-bg` 是 :root 既有 token 且與站上其他 4 處同一寫法）／動畫 >400ms ✗無（整段沒有 transition/animation）／假資料佔位 ✗無／置中對齊長段落 ✗無（沒有 text-align:center）／陰影堆疊 ✗無（整段 0 個 box-shadow）／`--up`/`--down` 未被誤用 ✓

**功能實測（headless Chrome 150.0.7871.184，`Network.setCacheDisabled=true` ＋ `?v=` cache-buster ＋ `Page.reload{ignoreCache}`；已確認 dist bundle 含最新文案：`grep '120 組裡只有 18 組過關' dist/assets/index-ClWy2B6U.js` = 1）**
- 375／768／1280：`documentElement.scrollWidth == clientWidth == innerWidth`（375/768/1280），`.help` 底下逐一掃 `getBoundingClientRect`，**溢出元素 0 個** ✓
- 目錄 7 個連結全部 44px 高；點 s4／s7／s1 後對應段 `getBoundingClientRect().top` 都是 0（精準對位）✓
- 「回選股頁」鈕 112×44，點了 hash 變空、`.hero` 出現、`.help` 消失 ✓（頁首與頁尾各一顆，都可用）
- 瀏覽器上一頁 → 回到 `#help` 且渲染說明書；下一頁 → 回主頁 ✓
- hero 入口點擊 → `#help` ✓；`#help` 狀態下 `ignoreCache` 重新整理 → 仍在說明書（h1「這個網站怎麼用」）✓

---

## 建議修復順序
1. 🔴1 副標的成本宣稱（改字最快，但不改就等於在空頭市場給錯的加碼訊號）
2. 🔴2 RS 定義拆兩條
3. 🟡3 記號圖例、🟡5 漏斗第 3/4 層講法、🟡4 檔數語意
4. 🟡8 觸控 44px、🟡6 籌碼 39 天、🟡7 綠燈沒橫幅
5. 🟢 群（數字精修）＋🟡10（既有 CSS 債，另開任務）
