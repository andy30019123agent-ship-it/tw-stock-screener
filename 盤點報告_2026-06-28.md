# tw-stock-screener 盤點報告

> 盤點日期：2026-06-28（台北時間）
> 執行：只讀分析，未改程式碼，未 commit/push。

---

## 快速總覽

| 嚴重度 | 數量 |
|--------|------|
| 🔴 高  | 0 |
| 🟡 中  | 4 |
| 🟢 低  | 6 |

**Build**：✅ 通過（`npm run build` 320KB JS gzip 103KB）
**npm audit**：✅ 0 vulnerabilities（上次 2026-06-21 的 esbuild 漏洞已消失）
**git 衛生**：✅ 乾淨，working tree clean，無敏感檔入版控

---

## 🟡 中嚴重度問題

---

### 🟡 1. notify_tg.py — TG 防重複機制完全失效（舊問題，仍未修）

**位置**：`pipeline/notify_tg.py` 第 26 行 `data_date()` / 第 155 行閘門判斷

**問題**：`data_date()` 用 `s.get("ohlc")` 取最後 K 線日期，但 `build_data.py` 第 391 行已把 `ohlc` `pop()` 出 results 只存進 `charts/<id>.json`，因此 `screener.json` 裡所有股票的 `ohlc` 都不存在。結果 `dd` 永遠是 `None`，閘門條件 `if not args.force and dd and last and dd <= last` 永遠不成立 → **每次 workflow 跑完都會發 TG**。

**實際影響確認**：`pipeline/notify_state.json` 現在是 `{"last_notified": null}`，證明每次推播都沒有正確記錄日期，防重機制從未生效。換言之，只要 GH Actions 排程跑（含週末），TG 都會收到推播。

**怎麼修**：
```python
# data_date 改讀 screener.json 頂層的 updated 欄位（格式：YYYY-MM-DD HH:MM）
def data_date(d):
    updated = d.get("updated", "")
    return updated[:10] if updated else None

# main() 改成：
text, dd = build_message(d)  # 拆出去
# build_message 不再算 dd，dd 由 data_date(d) 取得
dd = data_date(d)
```
或最簡單：在 `send(text)` 前加：
```python
if not dd:
    print("無法取得資料日期，略過推播。")
    return
```

---

### 🟡 2. filters.js — chipLogic='or' 搭配任一 days=0 時篩選完全失效

**位置**：`src/lib/filters.js` 第 30–39 行 `applyFilters()`

**問題**：
```js
const fOk = c.foreignDays <= 0 || s.foreign_streak >= c.foreignDays
const tOk = c.trustDays <= 0 || s.trust_streak >= c.trustDays
```
當 `trustDays=0` 時，`tOk` 永遠是 `true`。若使用者選 `chipLogic='or'`，判斷變成 `!(fOk || true)` = 永遠不過濾 → **任何股票都通過**。

**觸發場景**：點擊「🏦 外資連買」快速套用（`foreignDays=3, trustDays=0`）後，再切換 chipLogic 到「任一即可」→ 結果顯示所有股票。

**怎麼修**：`or` 邏輯應只計算「有設定（> 0）」的條件：
```js
const activeConditions = []
if (c.foreignDays > 0) activeConditions.push(fOk)
if (c.trustDays > 0) activeConditions.push(tOk)
if (activeConditions.length > 0) {
  const pass = c.chipLogic === 'or'
    ? activeConditions.some(Boolean)
    : activeConditions.every(Boolean)
  if (!pass) return false
}
```

---

### 🟡 3. build_data.py — api_get / get_universe 是死碼，但繼續讀 FINMIND_TOKEN

**位置**：`pipeline/build_data.py` 第 26–97 行

**問題**：架構已改為全免費 TWSE/TPEX 資料源。`build_data.py` 的 `main()` 實際呼叫 `load_universe_file()`（讀 `universe.json`），從不呼叫 `get_universe()`。`api_get()` 也從未在 `main()` 流程中被呼叫。`build_universe.py` 同樣不 import `build_data`。

因此 `TOKEN = os.environ.get("FINMIND_TOKEN", "")` 與 `api_get()`、`get_universe()` 共約 50 行是死碼，但仍讓維護者誤以為每日流程還在消耗 FinMind 額度。

另外，`STATUS.md` 仍寫「資料：FinMind 一檔一檔抓」，與實際不符。

**影響**：
- 在 GitHub Actions 沒有設定 `FINMIND_TOKEN` Secret，目前 TOKEN="" 所以 api_get 即使被意外呼叫也只是不帶 token，不會崩潰。
- 主要是文件混淆與死碼，無功能性 bug。

**怎麼修**：
1. 刪除 `build_data.py` 中的 `TOKEN`、`MAX_QUOTA_FAILS`、`_quota_fails`、`api_get()`、`get_universe()` 共約 50 行。
2. 更新 `STATUS.md`：改寫為「資料：TWSE/TPEX 免費官方源，無 FinMind 依賴」。

---

### 🟡 4. TG 群組 Chat ID 硬編碼（舊問題，仍未修）

**位置**：`pipeline/notify_tg.py` 第 20 行

**問題**：`CHAT_ID = os.environ.get("TG_CHAT_ID", "-5127072553")` — 群組 ID 作為 fallback 寫死在原始碼中並進了版控。雖然 Chat ID 本身不是金鑰，但搭配 TG Bot Token 就能向此群組發訊，屬資安衛生問題。

**怎麼修**：移除 fallback，改為：
```python
CHAT_ID = os.environ.get("TG_CHAT_ID")
if not CHAT_ID:
    raise SystemExit("缺少環境變數 TG_CHAT_ID")
```
並在 GitHub Secrets 加 `TG_CHAT_ID`。

---

## 🟢 低嚴重度問題

---

### 🟢 5. notify_tg.py — notify_state 讀取未用 with（舊問題，仍未修）

**位置**：`pipeline/notify_tg.py` 第 174 行
`json.load(open(args.state, encoding="utf-8")).get(...)` 沒有用 `with`，靠 GC 關閉檔案。CPython 正常但不嚴謹。

**怎麼修**：`with open(args.state, encoding="utf-8") as f: last = json.load(f).get("last_notified")`

---

### 🟢 6. price.json 11MB 在版控中持續增長

**位置**：`pipeline/history/price.json`（11MB），`pipeline/history/chip.json`（1MB）

這兩個檔案設計上就是要 commit 進版控（workflow 靠 git push 保存每日歷史），`PRICE_WINDOW=110` 限制每檔保留 110 個交易日。目前 1,966 檔 × 110 日 × 6 欄，理論最大約 12–14MB，已接近穩態。

**觀察**：非 bug，但若 universe 再擴大（例如加非電子股），price.json 可能膨脹到 30MB+，git clone 與 Actions checkout 時間會增加。

**建議**：維持現狀，如未來 universe 擴大超過 3000 檔再考慮改用 Git LFS 或外部儲存。

---

### 🟢 7. CHIP_WINDOW=20 導致連買天數最多顯示 20 天

**位置**：`pipeline/history_store.py` 第 9 行 `CHIP_WINDOW = 20`

`foreign_streak` / `trust_streak` 最大只能反映 20 個交易日，若某股法人連買 25 天，只能顯示最多 20 天（但實際上 streak 也是在這 20 天內計算連續，所以 25 天中後 20 天若都是買，streak=20）。這是已知的資料視窗限制，不影響正常使用（實際連買 20 天以上的標的極少）。

**建議**：可改為 30（約 6 週），僅需一行改動，風險極低。

---

### 🟢 8. dispersion_pct 若為 null 會顯示「離散 null%」

**位置**：`src/components/ResultTable.jsx` 第 117 行

`{s.dispersion_pct}%` 在 JavaScript 中若 `dispersion_pct` 為 `null` 會渲染 `"null%"`。

**實際風險**：`build_data.py` 在 `compute_indicators` 結尾：`'dispersion_pct': round(disp_now * 100, 2) if disp_now is not None else None`；而 `disp_now = dispersion(len(closes)-1)`，若 len(closes)≥65（已保證）且所有 MA 都已定義，`disp_now` 通常不為 None。實際資料中幾乎不出現，但防禦性加上 `?? '—'` 更穩。

**怎麼修**：`<span className="disp">離散 {s.dispersion_pct ?? '—'}%</span>`

---

### 🟢 9. ma_rising 未包含 MA60（舊問題，仍未修，設計取捨）

**位置**：`pipeline/build_data.py` 第 158 行

`ma_rising = all(rising(s) for s in (ma5s, ma10s, ma20s))` 未驗 MA60。若 MA60 仍下彎，但 MA5/10/20 翻揚，`ma_rising=True`。`signal_ma` 另外要求 `bull_aligned`（MA5>10>20>60），有部分保護，但 MA60 本身可能仍在下降趨勢中。

**建議**：可視策略加 `ma60s` 進驗證，或加程式碼備註說明這是刻意設計。

---

### 🟢 10. STATUS.md 內容已過時

**位置**：`STATUS.md`

「資料：FinMind 一檔一檔抓（限有量電子股，約 111 檔主流）」已不符現況——架構已改為 TWSE/TPEX 全市場 1,966 檔免費源，FinMind 已退場。如有其他人（或未來的 Andy）靠這份文件了解專案，會被誤導。

**怎麼修**：更新 STATUS.md 的「上次做到哪」段落。

---

## 安全性確認（無問題）

- **FinMind Token**：只在 `build_data.py` 第 27 行讀取 env var，實際上已是死碼（api_get 不被呼叫），GitHub Secret 設定與否不影響運作。
- **TG_BOT_TOKEN**：`os.environ.get("TG_BOT_TOKEN")`，未寫死。GitHub Secret 正確使用。
- **前端 bundle**：確認 `dist/assets/index-*.js` 無 FINMIND、TOKEN 等敏感字串。
- **XSS**：React 自動 escape。`el.innerHTML = ''` 只清空節點，不插入外部資料。
- **git 追蹤**：`git ls-files | grep -iE 'env|secret|key|token'` 無輸出。`.gitignore` 覆蓋 `.env`、`.env.*`、`public/data/screener.json`、`public/data/charts/`。

---

## 成本/風控確認

- **FinMind 額度**：目前正常流程（`daily_update.py` + `build_data.py`）完全不使用 FinMind API，額度消耗為 0。
- **TWSE/TPEX 免費源**：每日 `daily_update.py` 打約 4 支免費端點，`build_data.py` 只讀本機 JSON，無額度風險。
- **任何訪客消耗額度？**：否。資料在 CI pipeline 算好後部署靜態 JSON，前端只讀靜態檔，無使用者觸發任何 API。
- **快取**：歷史資料已由 `history_store` 實現，每日只 append 最新一天，無重複抓取。

---

## 舊問題追蹤（2026-06-21 盤點後未修）

| 問題 | 當時嚴重度 | 本次狀態 |
|------|-----------|---------|
| notify_tg data_date bug | 🟡 | ❌ 仍未修，notify_state.json = null |
| TG Chat ID 硬編碼 | 🟡 | ❌ 仍未修 |
| notify_state open() 未用 with | 🟢 | ❌ 仍未修 |
| ma_rising 未驗 MA60 | 🟢 | ❌ 仍未修（設計決策） |
| esbuild 漏洞 | 🟡 | ✅ npm audit 0 vulnerabilities |
| .impeccable 誤入版控 | 🔴 | ✅ 已修 |

---

*報告由 Claude Sonnet 4.6 自動產生，2026-06-28 台北時間盤點。*
